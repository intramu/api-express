export enum ReturnTypes {}

export enum Gender {
    MALE = "MALE",
    FEMALE = "FEMALE",
}

export enum Language {
    ENGLISH = "ENGLISH",
}

export enum Role {
    CAPTAIN = "CAPTAIN",
    COCAPTAIN = "COCAPTAIN",
    PLAYER = "PLAYER",
    MASTER = "MASTER",
    WORKER = "WORKER",
}

export enum Visibility {
    PRIVATE = "PRIVATE",
    OPEN = "OPEN",
    CLOSED = "CLOSED",
}

export enum Status {
    ACTIVE = "ACTIVE",
    INACTIVE = "INACTIVE",
    INCOMPLETE = "INCOMPLETE",
    BANNED = "BANNED",
    GRADUATED = "GRADUATED",
}
