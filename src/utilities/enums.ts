export enum ReturnTypes {}

export enum Gender {
    MALE,
    FEMALE,
}

export enum Role {
    CAPTAIN = "CAPTAIN",
    PLAYER = "PLAYER"
}

export enum Visibility {
    PRIVATE = "PRIVATE",
    PUBLIC = "OPEN",
    CLOSED = "CLOSED"
}

export enum Status{
    ACTIVE = "ACTIVE",
    INACTIVE = "INACTIVE",
    INCOMPLETE = "INCOMPLETE",
    BANNED = "BANNED",
    GRADUATED = "GRADUATED"
}
